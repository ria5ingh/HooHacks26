import congressClient from "./congressClient";

/**

export async function getSponsoredLegislation(ID){
    const { data } = await congressClient.get("/member/" + ID,
        params: {}
    )
}
    */